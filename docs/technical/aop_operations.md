# aop_operations.py

This module implements the core mathematical operations for the Alphabet of Powers system.

## Key Functions

### `simplify_value(val: AoPValue) -> AoPValue`

Combines terms in an AoPValue that have the same exponent.

### `add_values(v1: AoPValue, v2: AoP极

```

[Response interrupted by a tool use result. Only one tool may be used at a time and should be placed at the end of the message.]
